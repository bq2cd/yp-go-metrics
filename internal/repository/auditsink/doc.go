// Package auditsink contains various implementations of [repository.AuditSink] interface.
// Among those are: file sink (audit event are written to a file) and
// HTTP sink (audit events are sent to a remote HTTP server).
package auditsink
