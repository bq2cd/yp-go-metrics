// Package repository contains specific implementations (and some interfaces) for data sources and data sinks.
// For example, in-memory storage, SQL storage, various audit sinks (file, HTTP) and so on.
// Essentially, this package represents "infrastructure/data access/persistence" layer of the application.
package repository
