// Package model contains various data structures representing (ideally) a business domain.
// In our case, we define here core metric data structures that are used across multiple layers.
// There include [Metric], [AuditEvent] and collection-like data structures, such as [MetricSet].
// Simple operations on those structures (e.g. aggregation) are also defined here.
package model
